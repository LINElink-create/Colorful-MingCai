import { describe, it } from 'vitest'

// E2E 测试占位：该测试用例预留给未来集成 Playwright / WXT 的端到端场景
// 场景示例：在真实浏览器中选中文本 -> 触发右键高亮 -> 刷新页面后恢复高亮 -> 导出并验证文件内容
describe.skip('highlight persist export flow', () => {
  it('covers selection highlight restore and export in browser e2e', () => {
    // 预留给后续 Playwright 或 WXT E2E 集成。
  })
})